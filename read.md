to run elastic search
1.open power shell
wsl -d Ubuntu

2.go to elastic folder
cd ~/elastic-start-local

3.docker compose
docker compose up -d
4.open port
http://localhost:5601
put password and username
